import React from 'react'
import { Loader } from '../components/Loader'
export const HomeScreen = () => {
    return (
        <div>
            <Loader/>
            <Loader/>
            <Loader/>
            <Loader/>
            <Loader/>
            <Loader/>
            <Loader/>
            <Loader/>
            <Loader/>
        </div>
    )
}
